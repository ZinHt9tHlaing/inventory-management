info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/user/login"
  body:
    type: json
    data: |-
      {
        "email": "user@gmail.com",
        "password": "password"
      }
  auth:
    type: bearer
    token: ""

scripts:
  post-response: |-
    if (res.getStatus() === 200) {
      const body = res.getBody();
      if (body && body.token) {
        bru.setVar("userToken", body.token);
      }
    }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
