info:
  name: reset password
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/auth/reset-password"
  body:
    type: json
    data: |-
      {
        "email": "muller.reggie@example.org",
        "password": "password",
        "password_confirmation": "password"
      }
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
