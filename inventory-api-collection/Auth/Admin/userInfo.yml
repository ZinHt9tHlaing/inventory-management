info:
  name: userInfo
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/admin/me"
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
