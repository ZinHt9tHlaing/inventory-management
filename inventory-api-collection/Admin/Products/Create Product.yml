info:
  name: Create Product
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/admin/products"
  body:
    type: json
    data: |-
      {
        "name": "Product 1",
        "price": 250,
        "sku": "product-1",
        "supplier_id": "01m0c6ef76jrga79a7zqe091y3"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
