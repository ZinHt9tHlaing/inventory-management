info:
  name: Get All Products
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/admin/products?limit=100"
  params:
    - name: search
      value: Okey
      type: query
      disabled: true
    - name: page
      value: "2"
      type: query
      disabled: true
    - name: limit
      value: "100"
      type: query
    - name: min_price
      value: "500"
      type: query
      disabled: true
    - name: max_price
      value: "700"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
