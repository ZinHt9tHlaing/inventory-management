info:
  name: Create Supplier
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/admin/suppliers"
  body:
    type: json
    data: |-
      {
        "name": "Test Supplier",
        "contact": "test-supplier@example.net"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
