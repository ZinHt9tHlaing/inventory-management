info:
  name: "Delete Inventory "
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/admin/inventories/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
