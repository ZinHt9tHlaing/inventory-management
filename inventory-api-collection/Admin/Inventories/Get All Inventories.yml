info:
  name: Get All Inventories
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/admin/inventories"
  params:
    - name: search
      value: Burley
      type: query
      disabled: true
    - name: page
      value: "3"
      type: query
      disabled: true
    - name: limit
      value: "3"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
