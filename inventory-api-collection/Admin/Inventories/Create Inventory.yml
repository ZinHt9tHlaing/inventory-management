info:
  name: Create Inventory
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/admin/inventories"
  body:
    type: json
    data: |-
      {
        "product_id": "01m0sps132521gdegypff9ctcb",
        "warehouse_id": "01m0sps14a2j9tkynqhp3zw3zg",
        "quantity": 50
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
