info:
  name: Update User
  type: http
  seq: 6

http:
  method: PUT
  url: "{{baseUrl}}/admin/users/:id"
  params:
    - name: id
      value: "6"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Test User Update",
        "email": "testuser@gmail.com",
        "password": "password",
        "role": "user"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
