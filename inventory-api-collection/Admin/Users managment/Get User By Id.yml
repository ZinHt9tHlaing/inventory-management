info:
  name: Get User By Id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/admin/users/:id"
  params:
    - name: id
      value: "4"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
