info:
  name: Create User
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/admin/users"
  body:
    type: json
    data: |-
      {
        "name": "Test User",
        "email": "testuser@gmail.com",
        "password": "password",
        "role": "user"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
