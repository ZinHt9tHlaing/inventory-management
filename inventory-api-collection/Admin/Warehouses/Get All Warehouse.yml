info:
  name: Get All Warehouse
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/admin/warehouses"
  params:
    - name: search
      value: Dusty
      type: query
      disabled: true
    - name: page
      value: "8"
      type: query
      disabled: true
    - name: limit
      value: "3"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
